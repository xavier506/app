<h3>Edit Bill of Lading</h3>
<div class="form-container">
  <%= simple_form_for([@order, @bill_of_lading], :html => {'data-abide' => ' ' }, validate: true ) do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</div>
